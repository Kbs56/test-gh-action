name: Test List Job ID

on:
  workflow_call:
  workflow_dispatch:

jobs:
  Explore-GH-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: |
          sleep 10s
          echo "Hi Mom"
